1. RUN THEN REPLACE ACTUAL FOLDER FOR GIT

npm run build




2. RUN BELOW TO UPDATE FINAL

# Stage all changes
git add .

# Ask for commit message
$msg = Read-Host "Commit message"

if (-not $msg) {
    Write-Host "Commit aborted: empty message"
    exit 1
}

git commit -m "$msg"
git push origin $(git rev-parse --abbrev-ref HEAD)

# Build the Vite app
npm run build



==================================================================

each time update reactapp:
npm run build
npx cap copy
npx cap sync

cd "C:\Users\User\Desktop\FoodPapaDan\my-modern-app\android"
